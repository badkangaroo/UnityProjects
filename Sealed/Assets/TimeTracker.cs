using UnityEngine;
using System.Collections;

public class TimeTracker : MonoBehaviour {
	// Use this for initialization
	void Start () {
		//timer = new GameTimer(5.0f);
		//timer.BeginTimer();
	}
	
	// Update is called once per frame
	void Update () {
	}
}
